# JWT_SECRET for authentication
JWT_SECRET=test-jwt-secret-key-for-testing-only

# MongoDB connection (can be overridden by mongodb-memory-server)
MONGODB_URI=mongodb://localhost:27017/finance_test

# Server port
PORT=5000

# Email configuration (optional for tests)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=test@example.com
EMAIL_PASS=test-password
EMAIL_FROM=noreply@example.com

# TrueLayer API (optional for tests - use test credentials)
TRUELAYER_CLIENT_ID=test-client-id
TRUELAYER_CLIENT_SECRET=test-client-secret
TRUELAYER_REDIRECT_URI=http://localhost:5000/api/truelayer/callback

# Node environment
NODE_ENV=test
